# Default owner — every PR auto-requests review from @danielrcurtis.
# When more contributors join, add path-specific entries below the
# wildcard line; later rules override earlier ones for the matched paths.

* @danielrcurtis
