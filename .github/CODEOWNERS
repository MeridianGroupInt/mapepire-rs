# Default owners — every PR auto-requests review from all three. Any one of
# them satisfies the code-owner review requirement, so a PR opened by one
# owner can still be approved by another.
#
# Add path-specific entries below the wildcard line; later rules override
# earlier ones for the matched paths.

* @danielrcurtis @sophneum @joshalon
